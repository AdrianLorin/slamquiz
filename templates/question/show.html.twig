{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>{% trans %}Text{% endtrans %}</th>
                <td>{{ question.text }}</td>
            </tr>
            <tr>
                <th>{% trans %}Created_at{% endtrans %}</th>
                <td>{{ question.createdAt ? question.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>{% trans %}Updated_at{% endtrans %}</th>
                <td>{{ question.updatedAt ? question.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('question_index') }}">{% trans %}back to list{% endtrans %}</a>

    <a class="btn btn-primary" id="edit" href="{{ path('question_edit', {'id': question.id}) }}">{% trans %}edit{% endtrans %}</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
