{% extends 'base.html.twig' %}

{% block title %}Adminsecure{% endblock %}

{% block body %}
    <h1>Adminsecure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminsecure.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ adminsecure.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ adminsecure.roles ? adminsecure.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ adminsecure.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': adminsecure.id}) }}">edit</a>

    {{ include('adminsecure/_delete_form.html.twig') }}
{% endblock %}
